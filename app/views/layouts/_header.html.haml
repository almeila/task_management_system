- if logged_in?
  = link_to "ログアウト", logout_path, method: :delete, class: 'btn btn-default navbar-btn pull-right'
- else
  = link_to "ログイン", login_path, class: 'btn btn-default navbar-btn pull-right'  
